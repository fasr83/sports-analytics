[build]
  base    = "."
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — todas las rutas van al index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
